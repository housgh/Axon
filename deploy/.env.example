# Copy this file to .env (docker compose loads .env automatically) and change the password
# before running this stack anywhere other than your own machine for a quick local look.
# See deploy/README.md's "Not for production" section - this compose file has no TLS and no
# secrets management; SQL_SA_PASSWORD is the only thing that's actually parameterized.
SQL_SA_PASSWORD=Ax0n!DemoPassw0rd
